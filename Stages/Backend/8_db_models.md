## Overview 

Make the Schema with Claude  schema.html in the db folder


and make the models of the schema to be used in the API later 
and run With `if __name__ == "__main__":` to initialize the Postgres Database with the Models of the Schema

these model will used later with db excuter of the API to CRUD any of these tables